# Church Helper Local AI Wrapper Plan

This is the live implementation plan for the church accessibility helper AI wrapper and UI cleanup. It intentionally lives in the repo because the user asked for the plan files to be created alongside the code and specs.

`01-map.md` is the shared context every executor loads with its task file. Read `02-roadmap.md` first, then take the next unblocked task, open that task plus `01-map.md`, and update the task status when done.

Status legend: `not-started | in-progress | blocked | done`

