node_modules/
.env
.DS_Store

# Persistent agent identities — local only, never published
.claude/agents/

# Orchestration decision log (OD-NNNN) — the reasoning behind the agent layer, so it is local for the
# same reason the agent layer is. Backed up outside the repo, since being ignored leaves it unversioned.
.claude/decisions/

# Session memory — local only, never published
Memory/

# Orchestration mandate — local only
AGENTS.md

# Everything the agents produce — reports, gate records, local rules, and the code map with its
# changelog queue. All of it is working material for how the repo gets built, not part of the app, so
# the whole folder stays local rather than us maintaining a list of which parts are published.
.agent/
