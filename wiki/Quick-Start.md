# Quick Start

1. Run `npm install`.
2. Run `npm start`.
3. Open `http://localhost:3000`.
4. Press `H` to hide the helper panel on the TV-facing screen.
5. Use manual lines first if the room is noisy or the browser speech engine is unavailable.
