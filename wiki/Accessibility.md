# Accessibility Notes

- Keep the TV display to five large lines only.
- Keep the helper panel easy to operate with keyboard shortcuts.
- Use `Text size` and `Margins` to adapt to the viewer's distance.
- Use `Update interval` to slow down or speed up summaries if the room pace changes.
- Do not rely on color alone for any meaning.
